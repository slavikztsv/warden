# Pinned exactly. Verified on these versions: 606 python tests, 53 rego tests,
# and both demo profiles end to end. NOT re-run under Docker on this machine
# (no daemon access) -- the compose profiles and tests/demo/test_isolation.sh are
# the one thing still to confirm after this bump.
#
# httpx stays on 0.28.x deliberately. Starlette's TestClient now warns that it
# wants `httpx2`; that is a new major package, the broker uses httpx for real
# outbound traffic as well as in tests, and swapping both at once is a
# migration rather than a bump. The warning is a deprecation, not a break.
fastapi==0.141.1
uvicorn[standard]==0.52.0
httpx==0.28.1
pyjwt[crypto]==2.13.0
pytest==9.1.1
pytest-asyncio==1.4.0
